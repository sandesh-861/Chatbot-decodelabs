# DecodeLabs Project 1: Custom Gemini Chatbot with Memory

Successfully completed the project requirements:
- Built a live text interface using Python and the official Google GenAI SDK.
- Designed an active session memory array to track context across multiple conversation turns.
- Implemented an automated Sliding Window validation gate to handle FIFO token optimization.